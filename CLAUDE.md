# 작업 지침

- 이 저장소에서 파일을 추가/수정한 뒤에는 별도 요청 없이 바로 `git add`, `git commit`, `git push origin main`까지 수행한다.
- 커밋 메시지는 변경 내용을 간결하게 요약하고, 마지막 줄에 `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`를 포함한다.
- 시크릿/자격증명으로 보이는 파일은 위 규칙과 무관하게 커밋하지 않고 사용자에게 확인한다.
